# Avocado Dataset Analysis

Short summary
- **Goal:** Explore avocado sales data and produce a short report (see `avocado_report.txt`) and analysis in `main.ipynb`.
- **Files:** `avocado.csv` (dataset), `main.ipynb` (analysis notebook), `avocado_report.txt` (text summary).

How to use
- Open `main.ipynb` in Jupyter/JupyterLab. The notebook should include data cleaning, EDA, and visualizations.
- Suggested packages: pandas, numpy, matplotlib, seaborn.

Notes & suggestions
- Confirm the date parsing and region columns are handled correctly in the notebook.
- Consider exporting key figures as images and adding a short README section summarizing main findings.
